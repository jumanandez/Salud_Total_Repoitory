<a href="{{ $href }}" class="flex flex-col items-center hover:opacity-80 transition">
    <img src="{{ asset($img) }}" alt="{{ $label }}" class="w-32 h-32 mb-2 rounded-xl p-3">
    <span class="text-[#0C1C3C] font-semibold">{{ $label }}</span>
</a>